<div class="small-4 small-centered columns">
  <h1>New Piece</h1>
  <%= simple_form_for(@piece) do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.button :submit %>
  <% end %>
</div>
